# 实现计划

- [x] 1. 编写 Bug Condition 探索测试
  - **Property 1: Bug Condition** - 状态表示不清晰和转换逻辑分散
  - **关键要求**: 此测试必须在未修复的代码上失败 - 失败证明 bug 存在
  - **不要尝试修复测试或代码**
  - **注意**: 此测试编码了预期行为 - 在实现修复后通过时将验证修复
  - **目标**: 暴露反例，证明 bug 存在
  - **作用域 PBT 方法**: 针对确定性 bug，将属性作用域限定为具体失败案例以确保可重现性
  - 测试实现细节来自设计文档中的 Bug Condition
  - 测试断言应匹配设计文档中的 Expected Behavior Properties
  - 在未修复的代码上运行测试
  - **预期结果**: 测试失败（这是正确的 - 证明 bug 存在）
  - 记录发现的反例以理解根本原因
  - 当测试编写完成、运行并记录失败时，标记任务完成
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. 编写 Preservation 保留属性测试（在实现修复前）
  - **Property 2: Preservation** - 现有窗口行为不变
  - **重要**: 遵循观察优先方法
  - 在未修复的代码上观察非 bug 输入的行为
  - 编写基于属性的测试，捕获设计文档 Preservation Requirements 中的观察行为模式
  - 基于属性的测试生成许多测试用例以提供更强保证
  - 在未修复的代码上运行测试
  - **预期结果**: 测试通过（确认要保留的基线行为）
  - 当测试编写完成、运行并在未修复代码上通过时，标记任务完成
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [ ] 3. 窗口状态管理系统重构

  - [x] 3.1 创建核心状态类型和接口
    - 创建 `功能/主窗口/状态/窗口状态.cs`
    - 定义 `WindowState` 枚举（NotCreated, Hidden, Windowed, Maximized, Pinned）
    - 定义 `StateTransition` 记录类型（FromState, ToState, Timestamp, Reason）
    - 定义 `StateChangedEventArgs` 事件参数类
    - 🔴 **CRITICAL**: 定义 `TransitionPlan` 记录类型，包含 `TransitionId` 字段防止竞态条件
    - 定义 `CompositeTransition` 记录类型（组合转换记录）
    - 定义 `IDispatcher` 接口及其实现（`WinUIDispatcher`, `SynchronousDispatcher`）
    - _Bug_Condition: 系统使用两个布尔值无法准确表示五种窗口状态_
    - _Expected_Behavior: 使用明确的 WindowState 枚举表示状态_
    - _Preservation: 不影响现有窗口行为_
    - _Requirements: 2.1_

  - [x] 3.2 实现 WindowStateManager 核心逻辑
    - 创建 `功能/主窗口/状态/窗口状态管理器.cs`
    - 实现 PendingState 和 CommittedState 机制
    - 实现 VisualState 和 LogicalState 属性（视觉/逻辑状态分离）
    - 实现基于版本号的并发控制（_transitionId）
    - 定义状态转换矩阵（支持直接转换：Pinned/Maximized -> Hidden）
    - 实现 `CanTransitionTo` 验证方法
    - 🔴 **CRITICAL**: 实现 `CreatePlan` 方法，返回包含 TransitionId 的 TransitionPlan
    - 🟠 **CRITICAL**: 在 `CreatePlan` 中，TryEnqueue 失败时立即回滚状态并返回 null
    - 实现 `CommitTransition(int transitionId)` 方法（验证版本号后提交）
    - 实现 `RollbackTransition(int transitionId, string? reason)` 方法（验证版本号后回滚）
    - 实现 OS 同步事件排队（`QueueSyncEvent`, `ProcessPendingSyncEvent`）
    - 实现组合转换记录（`RecordSubTransition`）
    - 实现状态转换历史记录（固定大小循环缓冲区，最多 100 条）
    - 实现 `GetTransitionHistory()` 和 `GetCompositeTransitions()` 返回副本（线程安全）
    - 实现 `IDisposable` 接口
    - 使用 `lock` 保证线程安全
    - _Bug_Condition: 状态转换逻辑分散，缺少验证机制_
    - _Expected_Behavior: 统一的状态管理器处理所有转换，转换前验证合法性_
    - _Preservation: 不影响现有窗口行为_
    - _Requirements: 2.2, 2.3, 2.5_

  - [x] 3.3 调整 MainWindowViewModel 架构
    - 修改 `功能/主窗口/状态/主窗口视图模型.cs`
    - 移除 WindowStateManager 所有权（不再持有 StateManager）
    - 添加 `CurrentState` 属性（WindowState 类型）
    - 实现 `SubscribeToStateManager` 方法（订阅 StateChanged 事件）
    - 实现 `UnsubscribeFromStateManager` 方法（取消订阅）
    - 实现 `OnStateChanged` 事件处理器（更新 CurrentState）
    - 移除 `IsWindowVisible` 和 `IsDockPinned` 属性（不保留兼容层）
    - 移除 `Dispose` 方法（不持有资源）
    - _Bug_Condition: 布局信息和 UI 状态分离，需要手动协调_
    - _Expected_Behavior: ViewModel 订阅状态变化，自动更新 UI_
    - _Preservation: 所有现有窗口行为保持不变_
    - _Requirements: 2.4_

  - [x] 3.4 重构 WindowHostController 状态管理
    - 修改 `功能/主窗口/显示隐藏/窗口宿主控制器.cs`
    - 添加 `_stateManager` 字段（持有所有权）
    - 在构造函数中创建 WindowStateManager
    - 订阅 `StateChanged` 事件
    - 调用 `_viewModel.SubscribeToStateManager(_stateManager)`
    - 🔴 **CRITICAL**: 实现 `OnWindowStateChanged` 事件处理器，使用命令模式
      - 从 `CreatePlan` 获取 TransitionPlan（包含 TransitionId）
      - 从 plan.TransitionId 获取版本号（不单独查询）
      - 执行副作用（支持组合副作用）
      - 添加超时保护（默认 2000ms）
      - 成功后调用 `CommitTransition(transitionId)`
      - 失败或超时后调用 `RollbackTransition(transitionId)`
      - 执行补偿操作（如果有）
    - 实现组合副作用方法（`ExecuteCompositeAsync`）
    - 实现各种副作用方法（`ExecuteHideAnimationAsync`, `ExecuteShowAnimationAsync`, 等）
    - 在 `OnWindowClosed` 中取消订阅并释放 StateManager
    - _Bug_Condition: 状态转换逻辑分散在多个方法中_
    - _Expected_Behavior: 统一的事件处理器协调所有状态转换_
    - _Preservation: 所有动画、AppBar、窗口样式逻辑保持不变_
    - _Requirements: 2.2, 2.3, 3.1, 3.2, 3.3, 3.6_

  - [x] 3.5 重构状态转换方法
    - 修改 `功能/主窗口/显示隐藏/窗口宿主控制器.cs`
    - 重构 `ToggleWindow` 方法（使用 CreatePlan，支持直接转换）
    - 重构 `TogglePinnedDock` 方法（使用 CreatePlan）
    - 重构 `ToggleMaximize` 方法（使用 CreatePlan）
    - 实现 `SyncFromOSWindowState` 方法（使用 QueueSyncEvent）
    - 添加防重入检查（`IsTransitioning`）
    - 移除 PendingAction 机制（不再需要）
    - _Bug_Condition: 状态转换逻辑分散，缺少统一入口_
    - _Expected_Behavior: 所有转换通过 StateManager 执行_
    - _Preservation: 用户交互行为完全不变_
    - _Requirements: 2.2, 2.5, 3.1, 3.2, 3.3_

  - [x] 3.6 更新 MainWindow.xaml.cs 事件处理
    - 修改 `功能/主窗口/入口/主窗口.xaml.cs`
    - 移除直接的 Presenter 操作（`ToggleWindowState` 方法）
    - 移除 unpin-before-maximize 逻辑（移到 Controller）
    - 移除 unpin-before-restore 逻辑（移到 Controller）
    - 更新 `OnWindowStateToggleRequested` 调用 `_windowController.ToggleMaximize()`
    - 更新 `OnDockToggleRequested` 调用 `_windowController.TogglePinnedDock()`
    - 保留 `AppWindow.Changed` 监听器，调用 `_windowController.SyncFromOSWindowState()`
    - 订阅 `StateChanged` 事件，更新 UI（图标、边距等）
    - 在 `OnWindowClosed` 中取消所有事件订阅
    - _Bug_Condition: 状态转换逻辑分散在 MainWindow 中_
    - _Expected_Behavior: 所有逻辑通过 Controller 统一处理_
    - _Preservation: 所有 UI 更新和事件处理行为不变_
    - _Requirements: 2.2, 3.3, 3.7_

  - [x] 3.7 迁移所有调用方到新 API
    - 搜索所有使用 `IsWindowVisible` 的代码
    - 搜索所有使用 `IsDockPinned` 的代码
    - 将所有调用方迁移到使用 `CurrentState` 枚举
    - 更新所有状态检查逻辑（使用枚举比较而非布尔值）
    - 验证没有遗漏的调用方
    - _Bug_Condition: 旧 API 使用布尔值表示状态_
    - _Expected_Behavior: 所有代码使用新的枚举 API_
    - _Preservation: 功能行为完全不变_
    - _Requirements: 2.1, 2.4_

  - [x] 3.8 验证 Bug Condition 探索测试现在通过
    - **Property 1: Expected Behavior** - 状态表示清晰和转换逻辑统一
    - **重要**: 重新运行任务 1 中的相同测试 - 不要编写新测试
    - 任务 1 中的测试编码了预期行为
    - 当此测试通过时，确认预期行为已满足
    - 运行任务 1 中的 Bug Condition 探索测试
    - **预期结果**: 测试通过（确认 bug 已修复）
    - _Requirements: 设计文档中的 Expected Behavior Properties_

  - [x] 3.9 验证 Preservation 测试仍然通过
    - **Property 2: Preservation** - 现有窗口行为不变
    - **重要**: 重新运行任务 2 中的相同测试 - 不要编写新测试
    - 运行任务 2 中的 Preservation 属性测试
    - **预期结果**: 测试通过（确认无回归）
    - 确认修复后所有测试仍然通过（无回归）

- [x] 4. Checkpoint - 确保所有测试通过
  - 确保所有测试通过，如有问题请询问用户
