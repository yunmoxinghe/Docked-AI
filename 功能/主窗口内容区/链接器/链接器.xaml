<UserControl
    x:Class="Docked_AI.Features.MainWindowContent.Linker.Linker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:navBar="using:Docked_AI.Features.MainWindowContent.NavigationBar"
    xmlns:contentArea="using:Docked_AI.Features.MainWindowContent.ContentArea"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="WidthStates">
            <VisualState x:Name="NarrowState">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="0" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="ContentHost.Margin" Value="4,6,4,4" />
                </VisualState.Setters>
            </VisualState>
            <VisualState x:Name="WideState">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="700" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="ContentHost.Margin" Value="16,6,16,4" />
                </VisualState.Setters>
            </VisualState>
            <VisualState x:Name="LargeState">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="1200" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="ContentHost.Margin" Value="24,6,24,4" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>

    <Grid Background="Transparent" Padding="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="48" />
        </Grid.ColumnDefinitions>

        <contentArea:ContentArea
            x:Name="ContentHost"
            Grid.Column="0"
            Margin="4,6,0,4" />

        <navBar:NavigationBar
            x:Name="NavBar"
            Grid.Column="2" />
    </Grid>
</UserControl>
