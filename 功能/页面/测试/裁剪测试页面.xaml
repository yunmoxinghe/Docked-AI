<Page
    x:Class="Docked_AI.Features.Pages.Test.ClipTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="#FFEF4444" />
        <Border Grid.Row="0" Grid.Column="1" Background="#FF3B82F6" />
        <Border Grid.Row="1" Grid.Column="0" Background="#FF22C55E" />
        <Border Grid.Row="1" Grid.Column="1" Background="#FFF59E0B" />
    </Grid>
</Page>
